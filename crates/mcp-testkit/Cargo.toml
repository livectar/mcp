[package]
name = "mcp-testkit"
version = "0.1.0"
edition = "2021"
license = "MIT"
repository = "https://github.com/livectar/mcp"

[dependencies]
async-trait.workspace = true
mcp-protocol = { path = "../mcp-protocol" }
mcp-sdk = { path = "../mcp-sdk" }
